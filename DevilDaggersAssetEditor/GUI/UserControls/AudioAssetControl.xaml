<UserControl x:Class="DevilDaggersAssetEditor.GUI.UserControls.AudioAssetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
	<Grid x:Name="Data">
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
			<!--<ColumnDefinition Width="2*" />-->
			<ColumnDefinition Width="4*" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Label Grid.Column="0" Content="{Binding AssetName}" />
		<TextBox Grid.Column="1" Name="TextBoxLoudness" Text="{Binding Loudness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		<!--<Label Grid.Column="2" Content="{Binding Description}" />-->
		<Label Grid.Column="2" Name="LabelEditorPath" Content="{Binding EditorPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" /> <!--TODO: Fix binding and remove Name-->
		<Button Grid.Column="3" Click="ButtonBrowsePath_Click">Browse path</Button>
	</Grid>
</UserControl>