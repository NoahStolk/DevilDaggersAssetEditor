<UserControl x:Class="DevilDaggersAssetEditor.GUI.UserControls.MenuBarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
	<Menu DockPanel.Dock="Top" Name="MenuPanel">
		<MenuItem Header="File" Name="FileMenuItem" />
		<MenuItem Header="Options">
			<MenuItem Header="Settings" Click="Settings_Click" />
		</MenuItem>
		<MenuItem Header="Help">
			<MenuItem Header="About" Click="About_Click" />
			<MenuItem Header="View source code" Click="SourceCode_Click" />
			<MenuItem Header="Check for updates" Name="UpdateItem" Click="Update_Click" />
			<MenuItem Header="Show log" Click="ShowLog_Click" />
		</MenuItem>
		<MenuItem Header="Compatibility">
			<MenuItem Header="Convert 0.2.0.0 mod file format" Click="ConvertModFile0200_Click" />
		</MenuItem>
	</Menu>
</UserControl>