<UserControl x:Class="DevilDaggersAssetEditor.GUI.UserControls.AssetControls.TextureAssetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
	<Grid x:Name="Data">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="4*" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Column="0" Text="{Binding AssetName}" />
		<TextBlock Grid.Column="1" Text="{Binding Description}" />
		<!--TODO: Fix binding and remove Name-->
		<TextBlock Grid.Column="2" Name="TextBlockEditorPath" Text="{Binding EditorPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		<Button Grid.Column="3" Click="ButtonBrowsePath_Click">Browse path</Button>
	</Grid>
</UserControl>